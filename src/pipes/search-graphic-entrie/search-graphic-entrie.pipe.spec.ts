import { SearchGraphicEntriePipe } from './search-graphic-entrie.pipe';

describe('SearchGraphicEntriePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchGraphicEntriePipe();
    expect(pipe).toBeTruthy();
  });
});
