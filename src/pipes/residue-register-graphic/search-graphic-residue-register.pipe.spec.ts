import { SearchGraphicResidueRegisterPipe } from './search-graphic-residue-register.pipe';

describe('SearchGraphicResidueRegisterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchGraphicResidueRegisterPipe();
    expect(pipe).toBeTruthy();
  });
});
